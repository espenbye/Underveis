# Xcode / xcodegen
# Underveis.xcodeproj is generated by xcodegen but committed so Xcode Cloud can build it
# (its runners can't install xcodegen). Regenerate with `make generate` and commit the result.
xcuserdata/
DerivedData/
build/
*.xcuserstate

# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/

# Claude Code (personal, per-project overrides)
.claude/settings.local.json

# Misc
.prettiercache
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
